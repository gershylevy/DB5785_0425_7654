-- קבלת כל הנתונים מהטבלאות במערכת

-- בחירת כל הנתונים מטבלת הלקוחות
SELECT * FROM Customer;

-- בחירת כל הנתונים מטבלת הכתובות
SELECT * FROM Address;

-- בחירת כל הנתונים מטבלת אנשי הקשר
SELECT * FROM Contact;

-- בחירת כל הנתונים מטבלת המסמכים של הלקוחות
SELECT * FROM CustomerDocument;

-- בחירת כל הנתונים מטבלת ההערות של הלקוחות
SELECT * FROM CustomerNote;

-- בחירת כל הנתונים מטבלת סגמנטי הלקוחות
SELECT * FROM CustomerSegment;

-- בחירת כל הנתונים מטבלת שיוך לקוחות לסגמנטים
SELECT * FROM CustomerSegmentAssignment;